import {
  FontAwesomeIcon,
  FontAwesomeIconProps,
} from '@fortawesome/react-fontawesome'

function Icon(iconProps: FontAwesomeIconProps) {
  return <FontAwesomeIcon {...iconProps} />
}

export default Icon
